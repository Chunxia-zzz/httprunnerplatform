# If you prefer the allow list template instead of the deny list, see community template:
# https://github.com/github/gitignore/blob/main/community/Golang/Go.AllowList.gitignore
#
# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Dependency directories (remove the comment below to include it)
# vendor/

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
# .idea/
# .vscode/

# ---------------------------------------------------------------
# httprunnerplatform 平台特有
# ---------------------------------------------------------------

# 平台运行期数据：用例工作区与执行产物，绝不入库
/data/workspaces/
/data/runtime/

# 本地配置（只提交 configs/config.example.yaml）
/configs/config.yaml

# hrp 引擎二进制：体积大且平台外置管理，不入库
# （*.exe 已在上方忽略，此处补充 Linux/macOS 无后缀二进制）
/hrp
/bin/hrp

# 前端
/web/node_modules/
/web/dist/
/web/.vite/
*.tsbuildinfo

# go:embed 的内嵌目录：内容由 scripts/build.sh 从 web/dist 同步而来。
# 只保留 .gitkeep，让全新克隆不装 Node 也能 go build / go test。
/internal/webui/dist/*
!/internal/webui/dist/.gitkeep

# 实测/调试产物
/stderr.log
/stdout.log
/tmp/

# 本地端到端冒烟的隔离实例：独立端口 + 独立 SQLite 库 + 独立工作区
# （scripts/smoke-suite.mjs 用，产物随时可删）
/.smoke/

# ---------------------------------------------------------------
# 引擎探针工程（testdata/）
# ---------------------------------------------------------------

# 探针工程的 .env 是夹具本体（提供 base_url），必须入库
# —— 覆盖上方全局的 `.env` 忽略规则
!testdata/**/.env

# hrp 在执行时写入项目根的运行期产物，一律不入库
results/
__pycache__/
.debugtalk_gen.py
