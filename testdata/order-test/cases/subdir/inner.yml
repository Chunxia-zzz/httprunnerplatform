config:
    name: "ORDER-SUBDIR-inner"
    verify: false
teststeps:
-
    name: "only step"
    request:
        method: GET
        url: $base_url/get
    validate:
        - eq: ["status_code", 200]
